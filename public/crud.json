{
    "fields": [
        {
            "name": "name",
            "type": "string"
        },
        {
            "name": "contact",
            "type": "string"
        },
        {
            "name": "email",
            "type": "email"
        },
        {
            "name": "deleted_at",
            "type": "string"
        }
    ],
    "validations": [
        {
            "field": "name",
            "rules": "required|string|min:5"
        },
        {
            "field": "contact",
            "rules": "required|numeric|min:9|max:9"
        },
        {
            "field": "email",
            "rules": "required|email|unique:contacts"
        }
    ]
}